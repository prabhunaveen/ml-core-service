apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ml-core-servire.fullname" . }}-test-connection"
  labels:
    {{- include "ml-core-servire.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ml-core-servire.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
